[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
# --ds pins the test settings even when a shell exports DJANGO_SETTINGS_MODULE for a dev server.
addopts = -q -p no:cacheprovider --ds=config.settings.test
filterwarnings =
    ignore::DeprecationWarning
