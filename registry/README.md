# Registry

The registry is the source of truth.

- `authors/`: stable author entities (`AUTH-####`)
- `objects/`: Tier-2 research objects (`SR-OBJ-######`)
- `sources/`: source records (`SRC-######`)
- `relations/`: typed relations (`REL-######`)

Do not edit generated public views in `site/` as source data.
